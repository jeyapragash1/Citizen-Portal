Flask==2.3.2
flask-cors==3.0.10
pymongo[srv]==4.4.1
dnspython==2.4.2
python-dotenv==1.0.1
Flask-Session==0.8.0
bcrypt==4.0.1
flask-login==0.6.3
# The heavy ML packages are optional. To avoid building Rust-backed wheels (e.g. tokenizers)
# on Windows, install ML requirements separately via `requirements-ml.txt` or use conda.
# sentence-transformers==2.2.2
# huggingface-hub==0.11.0
# faiss-cpu==1.12.0  # optional: install via conda-forge where possible
# Pin numpy to a wheel-known version for Windows to avoid local build; adjust if you use conda
numpy==2.3.4
# Optional / recommended for vector search and scheduling (see requirements-ml.txt)
apscheduler==3.10.1
requests==2.31.0
pytest==7.4.0
Flask-Limiter==2.9.2